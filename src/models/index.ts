export interface IEditorConfig {
    element: HTMLBaseElement
}

export interface ICharSize {
    width: number
    height: number
}

/*
interface IEditor {
    destroy(): void;
}

interface IKeyService {
    next(key: string): void;
}

interface ICaretService {
    moveTo(): void;
}

interface ISelectService {
    select(): void;
}

interface IInfoStorage {

}*/